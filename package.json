{
  "name": "design-critique-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:plugin",
    "build:plugin": "node scripts/build-plugin.mjs",
    "verify:plugin": "node scripts/verify-plugin.mjs",
    "prepublish:plugin": "npm run build:plugin && npm run verify:plugin",
    "dev:api": "tsx backend/api/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.117.0",
    "@types/node": "^22.15.3",
    "esbuild": "^0.25.4",
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
